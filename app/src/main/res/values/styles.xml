<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="payerLoginScreen"/>
    <style name="payerLoginScreen.Login">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginRight">@dimen/login_margin</item>
        <item name="android:layout_marginLeft">@dimen/login_margin</item>
        <item name="android:layout_marginTop">@dimen/login_button_margin_top</item>
    </style>

    <style name="payerLoginScreen.ForgotPassword">
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:paddingTop">18dp</item>
        <item name="android:textSize">@dimen/login_forgot_password</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:text">@string/login_forgot_password</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="payerLoginScreen.EditText">
        <item name="android:paddingTop">@dimen/login_padding_top</item>
        <item name="android:paddingBottom">@dimen/login_padding_bottom</item>
        <item name="android:paddingLeft">@dimen/login_padding_left</item>
        <item name="android:textSize">@dimen/login_text_size</item>
        <item name="android:textCursorDrawable">@drawable/search_cursor</item>
        <item name="android:textColorHint">#837e7e</item>
        <item name="android:inputType">textFilter|textNoSuggestions</item>
        <item name="android:background">@drawable/selector_login_background</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:imeOptions">actionDone</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="payerLoginScreen.EditText.Dynamic">
        <!-- Phone Specific styles -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginRight">@dimen/login_margin</item>
        <item name="android:layout_marginLeft">@dimen/login_margin</item>
        <item name="android:paddingRight">@dimen/sidebar_item_padding</item>
    </style>


    <style name="payerLoginScreen.EditText.Dynamic.Username">
        <item name="android:layout_marginBottom">@dimen/login_margin_bottom</item>
        <item name="android:layout_marginTop">@dimen/login_margin_top</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:paddingRight">@dimen/sidebar_item_padding</item>
    </style>

    <style name="payerLoginScreen.EditText.Dynamic.Email">
        <item name="android:layout_marginBottom">@dimen/login_margin_bottom</item>
        <item name="android:layout_marginTop">@dimen/login_margin_top</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:paddingRight">@dimen/sidebar_item_padding</item>
    </style>

    <style name="payerLoginScreen.EditText.Dynamic.Phone">
        <item name="android:layout_marginBottom">@dimen/login_margin_bottom</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:paddingRight">@dimen/sidebar_item_padding</item>
    </style>

    <style name="payerLoginScreen.EditText.Dynamic.Password">
        <item name="android:password">true</item>
        <item name="android:paddingRight">@dimen/sidebar_item_padding</item>
    </style>
    <style name="Theme.Toolbar.OverLay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize your theme here. -->
        <item name="titleTextColor">@color/textCorlorWhite</item>
    </style>

    <style name="TextAppearance.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/textCorlorFocused</item>
        <item name="android:textSize">20sp</item>
    </style>


    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/tabTextColor</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/AppTabTextAppearance</item>
    </style>

    <!-- for text -->
    <style name="AppTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="textAllCaps">false</item>
        <item name="android:textColor">@color/tabTextColor</item>
    </style>

    <declare-styleable name="CircleImageView">
        <attr name="civ_border_width" format="dimension" />
        <attr name="civ_border_color" format="color" />
        <attr name="civ_border_overlay" format="boolean" />
        <attr name="civ_fill_color" format="color" />
    </declare-styleable>
</resources>
